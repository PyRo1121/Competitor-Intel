{
  "name": "competitor-intel-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "lint": "cd ../.. && bun run lint:js",
    "format": "cd ../.. && bun run fmt",
    "test": "bun test"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "hono": "^4.11.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "eslint": "^9.0.0",
    "prettier": "^3.4.0"
  }
}
